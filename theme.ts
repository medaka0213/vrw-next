import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#004F8A",
      light: "#004F8A",
      dark: "#004F8A",
    }
  },
});

export default theme;
